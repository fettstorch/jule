{
  "name": "local-consumer",
  "private": true,
  "description": "Test-only consumer that installs the locally packed @fettstorch/jule tarball to prove the about-to-be-published package is usable.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@fettstorch/jule": "file:./jule.tgz"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
